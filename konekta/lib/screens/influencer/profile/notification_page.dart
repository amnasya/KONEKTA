import 'package:flutter/material.dart';
import '_dummy_page.dart';

class NotificationPage extends StatelessWidget {
  const NotificationPage({super.key});
  @override
  Widget build(BuildContext context) => const DummyPage(title: 'Notifications', icon: Icons.notifications_none);
}
