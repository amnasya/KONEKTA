import 'package:flutter/material.dart';
import '_dummy_page.dart';

class SecurityPage extends StatelessWidget {
  const SecurityPage({super.key});
  @override
  Widget build(BuildContext context) => const DummyPage(title: 'Security', icon: Icons.lock_outline);
}
